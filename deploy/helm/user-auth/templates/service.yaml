apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-auth.fullname" . }}
  labels:
    {{- include "user-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-auth.selectorLabels" . | nindent 4 }}
