apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upload.fullname" . }}-test-connection"
  labels:
    {{- include "upload.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "upload.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
